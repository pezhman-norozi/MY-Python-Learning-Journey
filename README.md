#  سفر یادگیری پایتون من

این ریپازیتوری، آرشیو کامل مسیر یادگیری پایتون من است که بر اساس دوره آموزشی «جادی میرمیرانی» در مکتب‌خونه می‌باشد.

این مجموعه، تمام تمرین‌ها و پروژه‌های کوچک من را از اولین اسکریپت `if/else` تا کار با APIها و برنامه‌نویسی شیءگرا در بر می‌گیرد.

## 🗺️ ساختار پروژه ها

پروژه‌ها به صورت مرحله به مرحله و بر اساس مفاهیم آموزشی دسته‌بندی شده‌اند:

* **`01-Python-Basics/`**:
    شامل مفاهیم پایه‌ای پایتون، کار با متغیرها، ورودی/خروجی و دستورات شرطی (`if`, `elif`, `else`).

* **`02-Loops-and-Control-Flow/`**:
    تمرکز بر روی حلقه‌های `for` و `while`، حلقه‌های تودرتو و کنترل جریان حلقه با `break` و `continue`.

* **`03-Data-Structures/`**:
    اسکریپت‌هایی برای کار با ساختارهای داده اصلی پایتون: لیست‌ها (Lists)، دیکشنری‌ها (Dictionaries) و مجموعه‌ها (Sets).

* **`04-Functions-and-Advanced/`**:
    ورود عمیق‌تر به نوشتن توابع، درک `*args`، توابع تودرتو، مدیریت پیشرفته خطا (`try/except/finally`) و مفهوم دکوراتورها (Decorators).

* **`05-OOP-and-Libraries/`**:
    شامل مفاهیم برنامه‌نویسی شیءگرا (OOP)، ارث‌بری (`Inheritance`) و پروژه‌های کوچکی که از کتابخانه‌های خارجی قدرتمند استفاده می‌کنند:
    * **`Bazi_hads.py`**: یک بازی کامل و ساختاریافته با توابع.
    * **`APIcall.py`**: کار با API و ماژول `requests`.
    * **`BSoup.py`**: وب اسکرپینگ با `BeautifulSoup`.
    * **`Edit_photo.py`**: پردازش تصویر پایه با `Pillow (PIL)`.
